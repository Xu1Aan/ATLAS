apiVersion: v1
kind: Service
metadata:
  name: geoserver
  namespace: sw-dev
  labels:
    app: geoserver
spec:
  type: ClusterIP
  selector:
    app: geoserver
  ports:
    - name: http
      port: 80
      targetPort: 8080
